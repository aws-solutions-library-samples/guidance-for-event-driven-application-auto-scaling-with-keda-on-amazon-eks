{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "QueryMetrics",
            "Effect": "Allow",
            "Action": "aps:QueryMetrics",
            "Resource": "arn:aws:aps:${AWS_REGION}:${ACCOUNT_ID}:workspace/${PROMETHEUS_WORKSPACE}"
        }
    ]
}
